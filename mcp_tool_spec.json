{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "name": "x402-cleanweb-agent",
  "version": "2.2.0",
  "protocol": "mcp-2024-11-05",
  "payment": {
    "protocol": "x402",
    "network": "Polygon, Base, Arbitrum",
    "supported_currencies": ["USDC"],
    "pricing_tiers": {
      "LIGHT": "0.001 USDC (Single Web Clean)",
      "STANDARD": "0.005 USDC (PDF / Batch Scrape)",
      "HEAVY": "0.010 USDC (Gemini AI YouTube Full Audio Summary)",
      "ONCHAIN": "0.020 USDC (EIP-712 Signed Attestation Calldata)"
    },
    "free_trial": {
      "enabled": true,
      "quota": 2,
      "type": "instant_sandbox"
    }
  },
  "tools": [
    {
      "name": "clean_web_content",
      "description": "Scrapes and converts any raw web page into clean, LLM-ready markdown, eliminating ads, navigation bars, cookie banners, and CSS noise.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The target website URL to scrape and clean."
          }
        },
        "required": ["url"]
      }
    },
    {
      "name": "clean_youtube_transcript",
      "description": "Extracts high-precision subtitles, transcripts, or AI-powered comprehensive summaries for any YouTube video using Google Gemini Flash intelligence.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "YouTube video URL."
          },
          "lang": {
            "type": "string",
            "description": "Target transcript language code (default: 'ko,en').",
            "default": "ko,en"
          }
        },
        "required": ["url"]
      }
    },
    {
      "name": "clean_pdf_research",
      "description": "Parses and extracts structured plain text and metadata from online PDF whitepapers and academic research papers.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "Direct URL pointing to a PDF document."
          },
          "max_pages": {
            "type": "integer",
            "description": "Maximum number of pages to parse (default: 30).",
            "default": 30
          }
        },
        "required": ["url"]
      }
    },
    {
      "name": "clean_batch_scrape",
      "description": "Concurrently scrapes and extracts markdown from multiple URLs in a single high-speed batch operation.",
      "parameters": {
        "type": "object",
        "properties": {
          "urls": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of URLs to scrape in parallel (up to 10 URLs)."
          }
        },
        "required": ["urls"]
      }
    },
    {
      "name": "get_pass_status",
      "description": "Checks the active status, remaining queries, and validity period for an agent wallet address or pass token.",
      "parameters": {
        "type": "object",
        "properties": {
          "agent_wallet": {
            "type": "string",
            "description": "Ethereum/Polygon address (0x...) or Lemon Squeezy Pass Token."
          }
        },
        "required": ["agent_wallet"]
      }
    }
  ]
}
